App\Entity\Category:
  category{1..5}:
    category: '<word()>'

App\Entity\Post:
  post_{1..50}:
    category: '@category*'
    title: '<words(3,true)>'
    body: '<words(150,true)>'
    createdAt: '<dateTimeBetween("-4 month", "now")>'
    updatedAt: '<dateTimeBetween("-4 days", "now")>'
